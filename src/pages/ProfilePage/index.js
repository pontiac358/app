import React from 'react';
import Profile  from '../../modules/Profile';

const ProfilePage = () => {

    const { ProfileContainer } = Profile.containers;

    return <ProfileContainer />
};

export default ProfilePage